"""Session MCP resource (read model exposure).

v0.1 — session data is accessible via the agent_get_session and
agent_list_sessions tools. This module holds the resource handler if
MCP resource exposure is wired in a future version.
"""

# TODO(v0.2): Expose sessions as MCP resources using mcp.Resource
